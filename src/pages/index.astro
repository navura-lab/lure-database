---
import BaseLayout from '../layouts/BaseLayout.astro';
import SearchHero from '../components/SearchHero.astro';
import SectionHeading from '../components/SectionHeading.astro';
import PortalSearch from '../components/PortalSearch.astro';
import LureCard from '../components/LureCard.astro';
import { fetchAllLures } from '../lib/fetch-all-lures';
import { groupLuresBySeries } from '../lib/group-lures';
import { getTypeSlug, getFishSlug } from '../lib/category-slugs';
import makerLogos from '../data/maker-logos.json';
import { guideArticles } from '../data/guides';
import { contentArticles } from '../data/articles/_index';
import { seasonalGuides } from '../data/seasonal-guides';
import { fishingMethods } from '../data/fishing-methods';
import { curatedRankings } from '../data/curated-rankings';
import { editorialReviews } from '../data/seo/editorials/_index';

const lures = await fetchAllLures();
const series = groupLuresBySeries(lures ?? []);
const totalColors = lures?.length ?? 0;

// ─── ルアータイプ集計（上位9件）───
const typeMap = new Map<string, { count: number; image: string | null }>();
for (const s of series) {
  if (!s.type) continue;
  const existing = typeMap.get(s.type);
  if (existing) {
    existing.count++;
    if (!existing.image && s.representative_image) existing.image = s.representative_image;
  } else {
    typeMap.set(s.type, { count: 1, image: s.representative_image });
  }
}
const topTypes = [...typeMap.entries()]
  .sort((a, b) => b[1].count - a[1].count)
  .slice(0, 9)
  .map(([name, { count, image }]) => ({ name, slug: getTypeSlug(name), count, image }));
const remainingTypeCount = typeMap.size - topTypes.length;

// ─── メーカー集計（上位9件）───
const mfrMap = new Map<string, { name: string; slug: string; count: number }>();
for (const s of series) {
  const e = mfrMap.get(s.manufacturer_slug);
  if (e) e.count++;
  else mfrMap.set(s.manufacturer_slug, { name: s.manufacturer, slug: s.manufacturer_slug, count: 1 });
}
const topMakers = [...mfrMap.values()]
  .sort((a, b) => b.count - a.count)
  .slice(0, 9);
const remainingMakerCount = mfrMap.size - topMakers.length;

// ─── 対象魚集計（上位9件）───
const fishMap = new Map<string, number>();
for (const s of series) {
  for (const f of s.target_fish) fishMap.set(f, (fishMap.get(f) || 0) + 1);
}
const topFish = [...fishMap.entries()]
  .sort((a, b) => b[1] - a[1])
  .slice(0, 9)
  .map(([name, count]) => ({ name, slug: getFishSlug(name), count }));
const remainingFishCount = fishMap.size - topFish.length;

// ─── ランキング集計（上位8件）───
const crossMap = new Map<string, { fishName: string; typeName: string; count: number }>();
for (const s of series) {
  if (!s.type) continue;
  for (const fish of s.target_fish) {
    const fishSlug = getFishSlug(fish);
    const typeSlug = getTypeSlug(s.type);
    const key = `${fishSlug}-${typeSlug}`;
    const existing = crossMap.get(key);
    if (existing) {
      existing.count++;
    } else {
      crossMap.set(key, { fishName: fish, typeName: s.type, count: 1 });
    }
  }
}
const topRankings = [...crossMap.entries()]
  .filter(([_, v]) => v.count >= 3)
  .sort((a, b) => b[1].count - a[1].count)
  .slice(0, 8)
  .map(([slug, v]) => ({ slug, fishName: v.fishName, typeName: v.typeName, count: v.count }));

// ─── ヒーロー用ルアー画像（カラー数上位20件の代表画像）───
// ─── ランダムピックアップ（ビルド時にシャッフル、8件）───
const shuffled = [...series].filter(s => s.representative_image);
for (let i = shuffled.length - 1; i > 0; i--) {
  const j = Math.floor(Math.random() * (i + 1));
  [shuffled[i], shuffled[j]] = [shuffled[j], shuffled[i]];
}
const randomPicks = shuffled.slice(0, 8);

// ─── 新着ルアー（直近30日、最大10件）───
const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);
const newThreshold = thirtyDaysAgo.toISOString();
const newArrivals = series
  .filter(s => s.created_at > newThreshold)
  .slice(0, 10);

// ─── 季節ガイド（各季節から代表1-2件、計6件）───
const seasonOrder = ['春', '夏', '秋', '冬'] as const;
const seasonEmoji: Record<string, string> = { '春': '🌸', '夏': '☀️', '秋': '🍂', '冬': '❄️' };
const featuredSeasons = seasonOrder.flatMap(season => {
  const items = seasonalGuides.filter(g => g.season === season);
  // 各季節から最大2件（シーバス＋もう1魚種を優先）
  const seabass = items.find(g => g.fish === 'シーバス');
  const others = items.filter(g => g.fish !== 'シーバス');
  return seabass ? [seabass, ...(others.length > 0 ? [others[0]] : [])] : items.slice(0, 1);
}).slice(0, 8);

// ─── 釣り方（人気順6件）───
const featuredMethods = fishingMethods.slice(0, 6);

// ─── 人気比較ページ（キュレーテッドランキングから上位4件）───
const popularCompareKeys = Object.keys(curatedRankings).slice(0, 4);
const popularCompares = popularCompareKeys.map(slug => {
  const entry = crossMap.get(slug);
  return entry ? { slug, fishName: entry.fishName, typeName: entry.typeName, count: entry.count } : null;
}).filter((c): c is NonNullable<typeof c> => c !== null);

// ─── エディトリアルレビューピックアップ（最新6件）───
const reviewedLures = Object.entries(editorialReviews)
  .map(([slug, ed]) => {
    const s = series.find(s => s.slug === slug);
    return s ? { series: s, catchcopy: ed.catchcopy } : null;
  })
  .filter((r): r is NonNullable<typeof r> => r !== null)
  .slice(0, 6);

// ─── JSON-LD: WebSite schema ───
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'CAST/LOG',
  url: 'https://www.castlog.xyz',
  description: '釣り人のためのルアーデータベース。国内主要メーカーのルアーを網羅的に掲載。',
  inLanguage: 'ja',
  potentialAction: {
    '@type': 'SearchAction',
    target: {
      '@type': 'EntryPoint',
      urlTemplate: 'https://www.castlog.xyz/search/?q={search_term_string}',
    },
    'query-input': 'required name=search_term_string',
  },
};
---

<BaseLayout
  title={`CAST/LOG - ${series.length}シリーズ・${totalColors.toLocaleString()}カラーのルアーデータベース`}
  description={`国内${mfrMap.size}メーカーのルアーを網羅的に掲載。${series.length}シリーズ・${totalColors.toLocaleString()}カラー登録。ミノー、クランク、ワームなどタイプ別、メーカー別、対象魚別に検索可能。`}
  canonicalPath="/"
  jsonLd={jsonLd}
>
  <!-- ブランドヒーロー -->
  <SearchHero seriesCount={series.length} colorCount={totalColors} />

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-8 space-y-12">

    <!-- ポータル検索 -->
    <section class="animate-fade-up">
      <PortalSearch
        types={topTypes}
        makers={topMakers}
        fish={topFish}
        remainingTypeCount={remainingTypeCount}
        remainingMakerCount={remainingMakerCount}
        remainingFishCount={remainingFishCount}
        makerLogos={makerLogos as Record<string, string>}
      />
    </section>

    <!-- 新着ルアー -->
    {newArrivals.length > 0 && (
      <section class="animate-fade-up">
        <SectionHeading title="新着ルアー" href="/new/" slashLabel="NEW ARRIVALS" />
        <div class="relative">
          <div class="absolute left-0 top-0 bottom-4 w-8 bg-gradient-to-r from-bg to-transparent z-10 pointer-events-none"></div>
          <div class="absolute right-0 top-0 bottom-4 w-8 bg-gradient-to-l from-bg to-transparent z-10 pointer-events-none"></div>
          <div class="flex gap-4 overflow-x-auto pb-4 -mx-4 px-4 snap-x snap-mandatory scrollbar-hide animate-stagger">
            {newArrivals.map((s, i) => (
              <div class="snap-start shrink-0 w-56 sm:w-64">
                <LureCard series={s} isNew={true} priority={i < 4} />
              </div>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- ランキング -->
    {topRankings.length > 0 && (
      <section class="animate-fade-up">
        <SectionHeading title="カタログ" href="/ranking/" slashLabel="CATALOG" />
        <div class="section-cool rounded-2xl p-6 sm:p-8 -mx-4 sm:mx-0">
          <div class="grid grid-cols-2 sm:grid-cols-4 gap-3 animate-stagger">
            {topRankings.map(r => (
              <a
                href={`/ranking/${r.slug}/`}
                class="flex items-center justify-between px-4 py-3 bg-white rounded-lg border border-border-light hover:border-accent hover:shadow-sm transition-all duration-[0.15s] group"
              >
                <div>
                  <p class="text-[13px] font-bold text-text group-hover:text-accent transition-colors duration-[0.15s]">
                    {r.fishName}×{r.typeName}
                  </p>
                  <p class="font-mono text-[11px] text-text-dim mt-0.5">
                    {r.count}シリーズ
                  </p>
                </div>
                <span class="font-mono text-[11px] font-bold text-accent">{r.count}</span>
              </a>
            ))}
          </div>
          <div class="mt-3 text-right">
            <a href="/ranking/" class="font-mono text-[11px] text-text-dim hover:text-accent tracking-[1px] transition-colors duration-[0.15s]">
              全{crossMap.size}カテゴリを見る →
            </a>
          </div>
        </div>
      </section>
    )}

    <!-- エディトリアルレビュー -->
    {reviewedLures.length > 0 && (
      <section class="animate-fade-up">
        <SectionHeading title="エディトリアルレビュー" href="/article/" slashLabel="REVIEWS" />
        <div class="section-cool rounded-2xl p-6 sm:p-8 -mx-4 sm:mx-0">
          <div class="grid grid-cols-2 sm:grid-cols-3 gap-4 animate-stagger">
            {reviewedLures.map(r => (
              <div>
                <LureCard series={r.series} isNew={false} />
                <p class="text-[12px] text-text-mid mt-1.5 line-clamp-1 text-center px-1">
                  {r.catchcopy}
                </p>
              </div>
            ))}
          </div>
        </div>
      </section>
    )}

    <!-- ルアーを発見する（ビルド時ランダム） -->
    <section class="animate-fade-up">
      <div class="bg-[#111] rounded-2xl p-6 sm:p-8 -mx-4 sm:mx-0">
        <div class="flex items-end justify-between mb-4">
          <div>
            <p class="font-mono text-[10px] font-bold text-accent tracking-[2px] uppercase mb-1">/DISCOVER</p>
            <h2 class="text-[18px] font-bold text-white">ランダムで探す</h2>
          </div>
          <a
            href="/search/"
            class="inline-flex items-center gap-2 font-mono text-[11px] font-bold text-gray-400 tracking-[1px] px-4 py-2 border border-gray-700 rounded transition-all duration-[0.15s] hover:border-accent hover:text-white"
          >
            もっと見る →
          </a>
        </div>
        <div class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4 animate-stagger">
          {randomPicks.map(s => (
            <LureCard series={s} isNew={false} />
          ))}
        </div>
      </div>
    </section>

  </div>

</BaseLayout>

<!-- ランダム表示はビルド時SSGに変更（API不要） -->
