---
import BaseLayout from '../layouts/BaseLayout.astro';
import SearchHero from '../components/SearchHero.astro';
import SectionHeading from '../components/SectionHeading.astro';
import CategoryTile from '../components/CategoryTile.astro';
import LureCard from '../components/LureCard.astro';
import { fetchAllLures } from '../lib/fetch-all-lures';
import { groupLuresBySeries } from '../lib/group-lures';
import { getTypeSlug, getFishSlug } from '../lib/category-slugs';

const lures = await fetchAllLures();
const series = groupLuresBySeries(lures ?? []);
const totalColors = lures?.length ?? 0;

// ─── ルアータイプ集計（上位9件）───
const typeMap = new Map<string, number>();
for (const s of series) {
  if (s.type) typeMap.set(s.type, (typeMap.get(s.type) || 0) + 1);
}
const topTypes = [...typeMap.entries()]
  .sort((a, b) => b[1] - a[1])
  .slice(0, 9)
  .map(([name, count]) => ({ name, slug: getTypeSlug(name), count }));
const remainingTypeCount = typeMap.size - topTypes.length;

// ─── メーカー集計（上位9件）───
const mfrMap = new Map<string, { name: string; slug: string; count: number }>();
for (const s of series) {
  const e = mfrMap.get(s.manufacturer_slug);
  if (e) e.count++;
  else mfrMap.set(s.manufacturer_slug, { name: s.manufacturer, slug: s.manufacturer_slug, count: 1 });
}
const topMakers = [...mfrMap.values()]
  .sort((a, b) => b.count - a.count)
  .slice(0, 9);
const remainingMakerCount = mfrMap.size - topMakers.length;

// ─── 対象魚集計（上位9件）───
const fishMap = new Map<string, number>();
for (const s of series) {
  for (const f of s.target_fish) fishMap.set(f, (fishMap.get(f) || 0) + 1);
}
const topFish = [...fishMap.entries()]
  .sort((a, b) => b[1] - a[1])
  .slice(0, 9)
  .map(([name, count]) => ({ name, slug: getFishSlug(name), count }));
const remainingFishCount = fishMap.size - topFish.length;

// ─── 新着ルアー（直近30日、最大10件）───
const thirtyDaysAgo = new Date();
thirtyDaysAgo.setDate(thirtyDaysAgo.getDate() - 30);
const newThreshold = thirtyDaysAgo.toISOString();
const newArrivals = series
  .filter(s => s.created_at > newThreshold)
  .slice(0, 10);

// ─── API token（ランダムセクション用）───
const apiToken = import.meta.env.LDB_API_TOKEN || '';

// ─── JSON-LD: WebSite schema ───
const jsonLd = {
  '@context': 'https://schema.org',
  '@type': 'WebSite',
  name: 'ルアーDB',
  url: 'https://www.lure-db.com',
  description: '釣り人のためのルアーデータベース。国内主要メーカーのルアーを網羅的に掲載。',
  inLanguage: 'ja',
};
---

<BaseLayout
  title="ルアーDB - ルアーデータベース"
  description={`国内主要メーカーのルアーを網羅的に掲載。${series.length}シリーズ・${totalColors.toLocaleString()}カラー登録。`}
  canonicalPath="/"
  jsonLd={jsonLd}
>
  <!-- 検索ヒーロー -->
  <SearchHero seriesCount={series.length} colorCount={totalColors} />

  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8 py-6 space-y-8 sm:space-y-10">

    <!-- ルアータイプから探す -->
    <section>
      <SectionHeading title="ルアータイプから探す" href="/type/" />
      <div class="grid grid-cols-5 lg:grid-cols-10 gap-1.5 sm:gap-3">
        {topTypes.map(t => (
          <CategoryTile label={t.name} href={`/type/${t.slug}/`} count={t.count} variant="type" />
        ))}
        <CategoryTile label="その他" href="/type/" count={remainingTypeCount} variant="more" />
      </div>
    </section>

    <!-- メーカーから探す -->
    <section>
      <SectionHeading title="メーカーから探す" href="/maker/" />
      <div class="grid grid-cols-5 lg:grid-cols-10 gap-1.5 sm:gap-3">
        {topMakers.map(m => (
          <CategoryTile label={m.name} href={`/${m.slug}/`} count={m.count} variant="maker" />
        ))}
        <CategoryTile label="その他" href="/maker/" count={remainingMakerCount} variant="more" />
      </div>
    </section>

    <!-- 対象魚から探す -->
    <section>
      <SectionHeading title="対象魚から探す" href="/fish/" />
      <div class="grid grid-cols-5 lg:grid-cols-10 gap-1.5 sm:gap-3">
        {topFish.map(f => (
          <CategoryTile label={f.name} href={`/fish/${f.slug}/`} count={f.count} variant="fish" />
        ))}
        <CategoryTile label="その他" href="/fish/" count={remainingFishCount} variant="more" />
      </div>
    </section>

    <!-- 新着ルアー -->
    {newArrivals.length > 0 && (
      <section>
        <SectionHeading title="新着ルアー" href="/search/" />
        <div class="flex gap-4 overflow-x-auto pb-4 -mx-4 px-4 snap-x snap-mandatory scrollbar-hide">
          {newArrivals.map((s, i) => (
            <div class="snap-start shrink-0 w-56 sm:w-64">
              <LureCard series={s} isNew={true} priority={i < 4} />
            </div>
          ))}
        </div>
      </section>
    )}

    <!-- ランダムで探す -->
    <section>
      <div class="flex items-baseline justify-between mb-3 sm:mb-4">
        <h2 class="text-base sm:text-lg font-bold text-gray-900">ランダムで探す</h2>
        <button
          id="shuffle-btn"
          class="inline-flex items-center gap-1.5 text-xs sm:text-sm text-primary hover:text-primary/80 transition-colors"
        >
          <svg class="w-4 h-4" id="shuffle-icon" fill="none" viewBox="0 0 24 24" stroke="currentColor" stroke-width="2">
            <path stroke-linecap="round" stroke-linejoin="round" d="M4 4v5h.582m15.356 2A8.001 8.001 0 004.582 9m0 0H9m11 11v-5h-.581m0 0a8.003 8.003 0 01-15.357-2m15.357 2H15" />
          </svg>
          シャッフル
        </button>
      </div>
      <div id="random-grid" class="grid grid-cols-2 sm:grid-cols-3 lg:grid-cols-4 gap-4">
        <!-- Client-side rendered -->
      </div>
      <div id="random-spinner" class="py-8 text-center">
        <div class="inline-block h-5 w-5 animate-spin rounded-full border-2 border-sky-500 border-t-transparent"></div>
      </div>
    </section>

  </div>

  <script define:vars={{ apiToken }}>
    window.__LDB_T = apiToken;
  </script>
</BaseLayout>

<style>
  .scrollbar-hide {
    -ms-overflow-style: none;
    scrollbar-width: none;
  }
  .scrollbar-hide::-webkit-scrollbar {
    display: none;
  }
</style>

<script>
  const TOKEN = (window as any).__LDB_T || '';
  const grid = document.getElementById('random-grid')!;
  const spinner = document.getElementById('random-spinner')!;
  const shuffleBtn = document.getElementById('shuffle-btn')!;
  const shuffleIcon = document.getElementById('shuffle-icon')!;

  interface CardData {
    slug: string;
    name: string;
    manufacturer: string;
    manufacturer_slug: string;
    type: string;
    representative_image: string | null;
    color_count: number;
    price_min: number;
    price_max: number;
    is_new: boolean;
    has_limited: boolean;
  }

  function esc(s: string): string {
    return s.replace(/&/g,'&amp;').replace(/</g,'&lt;').replace(/>/g,'&gt;').replace(/"/g,'&quot;');
  }

  function cardHtml(d: CardData): string {
    const hasPrice = d.price_max > 0;
    const price = hasPrice
      ? d.price_min === d.price_max
        ? `¥${d.price_min.toLocaleString()}`
        : `¥${d.price_min.toLocaleString()} - ¥${d.price_max.toLocaleString()}`
      : '';

    const img = d.representative_image
      ? `<img src="${esc(d.representative_image)}" alt="${esc(d.name)}" loading="lazy" width="500" height="375" class="w-full h-full object-scale-down p-3">`
      : `<div class="placeholder-lure-bg w-full h-full flex items-center justify-center"><span class="text-xs text-sky-400">${esc(d.type)}</span></div>`;

    const badges = [
      d.is_new ? `<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border bg-amber-50 text-amber-700 border-amber-200">新着</span>` : '',
      d.has_limited ? `<span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border bg-purple-50 text-purple-700 border-purple-200">限定</span>` : '',
    ].filter(Boolean).join('');

    return `<a href="/${esc(d.manufacturer_slug)}/${esc(d.slug)}/" class="group block">
      <div class="bg-white rounded-xl border border-gray-100 overflow-hidden shadow-card transition-all duration-200 group-hover:shadow-card-hover group-hover:scale-[1.01]">
        <div class="aspect-[4/3] relative overflow-hidden bg-white">
          ${img}
          <div class="absolute top-2 right-2 flex flex-col gap-1">${badges}</div>
        </div>
        <div class="p-4">
          <p class="text-xs text-text-secondary tracking-wider uppercase mb-1">${esc(d.manufacturer)}</p>
          <h3 class="text-base font-bold text-gray-900 mb-1.5 group-hover:text-primary transition-colors">${esc(d.name)}</h3>
          <div class="flex items-center gap-1.5 text-xs text-text-secondary mb-3">
            <span class="inline-flex items-center px-2.5 py-0.5 rounded-full text-xs font-medium border bg-sky-50 text-sky-700 border-sky-200">${esc(d.type)}</span>
            <span>${d.color_count}カラー</span>
          </div>
          ${price ? `<p class="text-sm font-bold text-gray-900">${price}</p>` : ''}
        </div>
      </div>
    </a>`;
  }

  async function loadRandom() {
    spinner.classList.remove('hidden');
    shuffleIcon.classList.add('animate-spin');

    try {
      const res = await fetch('/api/catalog?sort=random&limit=8&page=0', {
        headers: { 'X-LDB-Token': TOKEN, 'Accept': 'application/json' },
      });
      if (!res.ok) throw new Error('API error');
      const data = await res.json();

      grid.innerHTML = '';
      const fragment = document.createDocumentFragment();
      const div = document.createElement('div');
      for (const item of data.items as CardData[]) {
        div.innerHTML = cardHtml(item);
        const el = div.firstElementChild;
        if (el) fragment.appendChild(el);
      }
      grid.appendChild(fragment);
    } catch {
      grid.innerHTML = '<p class="text-sm text-text-secondary col-span-full text-center py-4">読み込みに失敗しました</p>';
    } finally {
      spinner.classList.add('hidden');
      shuffleIcon.classList.remove('animate-spin');
    }
  }

  shuffleBtn.addEventListener('click', loadRandom);

  // 初回ロード
  loadRandom();
</script>
